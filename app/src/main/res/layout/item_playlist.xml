<?xml version="1.0" encoding="utf-8"?><!--
    Item layout for Playlist in RecyclerView -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Playlist Cover Image -->
        <ImageView
            android:id="@+id/imgPlaylistCover"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:contentDescription="@string/playlist_cover"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_playlist_placeholder" />

        <!-- Playlist Name -->
        <TextView
            android:id="@+id/txtPlaylistName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/imgPlaylistCover"
            app:layout_constraintTop_toTopOf="@id/imgPlaylistCover"
            tools:text="Today's Top Hits" />

        <!-- Playlist Description -->
        <TextView
            android:id="@+id/txtPlaylistDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/gray"
            android:textSize="13sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/imgPlaylistCover"
            app:layout_constraintTop_toBottomOf="@id/txtPlaylistName"
            tools:text="Ed Sheeran is on top of the Hottest 50!" />

        <!-- Song Count -->
        <TextView
            android:id="@+id/txtSongCount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:textColor="@color/gray_light"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/imgPlaylistCover"
            app:layout_constraintTop_toBottomOf="@id/txtPlaylistDescription"
            tools:text="50 songs" />

        <!-- Created By -->
        <TextView
            android:id="@+id/txtCreatedBy"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textColor="@color/gray_light"
            android:textSize="11sp"
            app:layout_constraintBottom_toBottomOf="@id/imgPlaylistCover"
            app:layout_constraintStart_toEndOf="@id/imgPlaylistCover"
            tools:text="By Spotify" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>